import React, { FunctionComponent } from "react";
import "./styles.scss";

const ZaapHrTitle: FunctionComponent = props => (
  <div className="c-zaap-hr-title">{props.children}</div>
);

export default ZaapHrTitle;
